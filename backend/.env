# Application
NODE_ENV=development
PORT=3000

# Database (using standard PostgreSQL environment variables)
PGHOST=localhost
PGPORT=6543
PGDATABASE=elysia-db
PGUSER=admin
PGPASSWORD=password
PGIDLE_TIMEOUT=0
PGCONNECT_TIMEOUT=30

# Docker Compose (these will be used directly in the docker-compose.yml)
POSTGRES_DB=elysia-db
POSTGRES_USER=admin
POSTGRES_PASSWORD=password